export type CharacterRules = {
    isYodaEnabled?: boolean,
    isObiWanEnabled?: boolean,
    isPadmeEnabled?: boolean,
    isQuiGonEnabled?: boolean,
    isAssassinEnabled?: boolean,
    isDarthSideousEnabled?: boolean,
    isSenatorPalpatineEnabled?: boolean,
    isAnakinEnabled?: boolean,
    isDarthMaulEnabled?: boolean,
    isJarJarEnabled?: boolean,
};

export type SpecialRules = {
    isTheSenateEnabled?: boolean,
    isTheForceEnabled?: boolean,
};
